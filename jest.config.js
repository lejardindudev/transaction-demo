
module.exports = {
  testEnvironment: 'node',
  testTimeout: 20000, // Temps max pour chaque test (en ms)
  verbose: true, // Affiche plus de détails lors des tests
  coverageDirectory: 'coverage', // Répertoire de génération des rapports de couverture de tests
};

